#include <SDL2/SDL.h>

SDL_Surface** cutSudoku(SDL_Surface* source) {
    SDL_Surface** surfacelist = calloc(9, sizeof(SDL_Surface*));
    int i = 0;
    int size = source->w / 3;
    SDL_Rect
    for (int x = 0; x < 3; ++x) {
        for (int y = 0; y < 3; ++y) {
            
        }
    }
}
